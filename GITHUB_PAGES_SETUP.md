# GitHub Pages 启用步骤

## 问题说明

GitHub Actions 部署失败，错误信息：
```
Get Pages site failed. Please verify that the repository has Pages enabled
and configured to build using GitHub Actions
```

这表示 GitHub Pages 还未启用。

---

## 解决方案：启用 GitHub Pages

### 步骤 1：进入仓库设置

1. **打开您的仓库**：https://github.com/mmc-cube/page_knowledge
2. **点击 Settings（设置）** 标签
3. 在左侧菜单找到 **Pages**

### 步骤 2：配置 GitHub Pages

在 Pages 设置页面中：

#### 方案 A：使用 GitHub Actions（推荐）✅

1. 在 **Source** 部分，选择 **GitHub Actions**
2. 点击 **Save** 保存

这会告诉 GitHub 使用 GitHub Actions 来构建和部署您的网站。

#### 方案 B：使用 gh-pages 分支

1. 在 **Source** 部分，选择 **Deploy from a branch**
2. 选择分支：**gh-pages**（如果没有这个分支，先跳过）
3. 点击 **Save** 保存

---

## 视觉指南

```
Settings → Pages
┌──────────────────────────────────┐
│ Source                           │
├──────────────────────────────────┤
│ ◯ Deploy from a branch           │
│ ◉ GitHub Actions    ← 选择这个  │
└──────────────────────────────────┘
    ↓
  [Save 按钮]
```

---

## 启用后的下一步

1. **保存设置后**，GitHub Actions 会自动重新运行部署
2. **稍等 1-2 分钟**，刷新 Actions 页面查看部署状态
3. **部署成功后**，您会看到 Pages 设置中显示网站 URL

---

## 完整 URL 访问地址

配置完成后，您的网站将在以下地址访问：

```
https://mmc-cube.github.io/page_knowledge/
```

---

## 验证部署成功

部署成功后，Pages 设置页面会显示：

```
✅ Your site is published at https://mmc-cube.github.io/page_knowledge/
```

---

## 常见问题

**Q: 为什么要选择 GitHub Actions？**
A: 因为我们已经创建了 `.github/workflows/deploy.yml`，它会自动构建和部署您的应用。

**Q: 启用后多久能访问网站？**
A: 通常需要 2-5 分钟。第一次部署可能需要稍长时间。

**Q: 如果还是失败怎么办？**
A:
1. 检查 Actions 标签页的错误日志
2. 确认已选择 "GitHub Actions" 作为 Source
3. 等待几分钟后手动重新运行工作流

---

## 手动触发重新部署

如果需要手动重新部署：

1. 访问：https://github.com/mmc-cube/page_knowledge/actions
2. 点击最新的 "Deploy to GitHub Pages" 工作流
3. 点击 "Re-run failed jobs" 或 "Re-run all jobs"

---

祝配置顺利！ 🚀
