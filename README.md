# 📚 期末复习站

一个高效的期末复习学习工具，支持混合队列（知识卡片 + 选择题）学习方式，内置 KaTeX 数学公式渲染。

## ✨ 特性

- **混合队列学习**：知识卡片与选择题按导入顺序混合出现
- **卡片翻面**：知识卡片可翻面查看背面和解析
- **选择题**：支持单选和多选，提交后显示对错和解析
- **数学公式**：集成 KaTeX，支持 LaTeX 行内和块级公式
- **移动端优先**：响应式设计，完美适配各种屏幕
- **内容驱动**：JSON 文件管理学习内容
- **强校验**：严格检验内容格式，提供清晰错误提示

## 🚀 快速开始

```bash
# 安装依赖
npm install

# 启动开发服务器（http://localhost:5173）
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 📋 内容管理

### 目录结构

```
/public/content/
  manifest.json
  subjects/
    math.json
    physics.json
```

### manifest.json

```json
{
  "version": "1.1",
  "title": "期末复习站",
  "subjects": [
    {
      "id": "math",
      "name": "高等数学",
      "path": "subjects/math.json"
    }
  ]
}
```

### 学科内容格式

#### 知识卡片（type: "card"）

```json
{
  "id": "math-card-001",
  "type": "card",
  "front": "导数定义？",
  "back": "$f'(x)=\\lim_{h\\to 0} \\frac{f(x+h)-f(x)}{h}$",
  "explain": "几何意义：切线斜率",
  "important": true
}
```

#### 选择题（type: "mcq"）

```json
{
  "id": "math-mcq-001",
  "type": "mcq",
  "stem": "下列哪个极限成立？",
  "multi": false,
  "options": [
    { "key": "A", "text": "$$\\lim_{x\\to 0}\\frac{\\sin x}{x}=0$$" },
    { "key": "B", "text": "$$\\lim_{x\\to 0}\\frac{\\sin x}{x}=1$$" }
  ],
  "answer": ["B"],
  "explain": "基本极限公式",
  "important": true
}
```

## 🔤 公式语法

使用 LaTeX 语法，由 KaTeX 自动渲染：

- **行内公式**：`$f(x) = x^2$`
- **块级公式**：`$$\int_a^b f(x)dx$$`

## 🛠️ 技术栈

- React 18 + Vite
- React Router v6 (HashRouter)
- KaTeX (数学公式)
- CSS 3 (深色模式支持)

## 📱 响应式设计

- 💻 桌面：1200px+
- 📱 平板：768px - 1199px
- 📱 手机：< 768px

## 🚀 部署到 GitHub Pages

1. 修改 `vite.config.js`：
```javascript
export default defineConfig({
  base: '/repo-name/',  // 替换为你的仓库名
})
```

2. 构建和部署：
```bash
npm run build
# 上传 dist 文件夹到 GitHub Pages
```

## 📖 项目结构

```
src/
├── pages/
│   ├── HomePage.jsx      # 首页
│   └── StudyPage.jsx     # 学习页
├── components/
│   ├── CardItem.jsx      # 卡片组件
│   ├── MCQItem.jsx       # 选择题组件
│   ├── ContentRenderer.jsx# 内容渲染
│   ├── Formula.jsx       # 公式渲染
│   └── ErrorMessage.jsx  # 错误提示
├── styles/
├── utils/
│   └── validation.js     # 验证工具
└── App.jsx
```

## 💡 最佳实践

1. 每个学科按学习顺序组织内容
2. 测试公式确保 KaTeX 兼容性
3. 对核心概念使用 `important: true`
4. 提供清晰的解析说明

---

**祝学习顺利！** 🎓
